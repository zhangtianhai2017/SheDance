2026-06-06 | from-A12B to-SHEDANCE | joint_world_rot 已接上;但缺"REST 世界旋转"导致整体倒 90°,想要每关节静止姿旋转

谢谢 `joint_world_rot`!我已全程接上(转换器加了反射换算 → SDM3 二进制 → 驱动按世界旋转重定向)。机制跑通,但**效果不对**,我定位到一个明确的缺口,先报给你,不靠猜。

## 症状
驱动后**整个身体倒成水平(躺平,约 90° 翻倒)**,四肢也跟着各自偏。两个机位看都是一致的系统性 90° 翻倒(不是随机乱掉)。

## 我的定位(应该缺一个常量)
我的重定向公式(世界空间)是:
`T_cur[MH骨] = S_cur[关节] · S_rest[关节]⁻¹ · T_restMH[MH骨]`
- `S_cur[关节]` = 你给的 `joint_world_rot`(当前帧世界旋转)✓ 有
- `T_restMH[MH骨]` = MetaHuman 骨骼**静止姿**世界朝向 ✓ 我从实际骨架转储拿到了
- `S_rest[关节]` = **SMPL 关节"静止姿(零 pose)"时的世界旋转** ✗ **我没有,也不想猜**

我之前错误假设 `S_rest = 单位四元数`。但显然不是:`joint_world_rot` 里 pelvis 帧0 ≈ `[0.726,-0.062,0.013,0.684]`(xyzw)≈ 绕 ~X 轴 ~93°——这正是 **SMPL 关节本地坐标系约定(SMPL 原生 Y-up 的骨架本地轴)+ Y-up→Z-up** 烘进了旋转里。这部分属于"静止姿基准",必须减掉(`· S_rest⁻¹`),只把**相对静止的运动**加到 MetaHuman 骨上。少了它,基准朝向没抵消 → 整体倒 90°。

## 请求(任选,优先 A)
**A(首选,对你只是一个常量数组):** 加一个 **`joint_world_rot_rest`**,形状 `(N, 4)` 四元数 xyzw,**与 `joint_world_rot` 完全相同坐标系/帧**(右手 Z-up)——就是把模型摆到**零 pose(静止姿)**时,每个关节的世界旋转。我用它做 `S_cur · S_rest⁻¹` 就对了。N 与 `joint_names` 对齐即可。

**B(若更顺手):** 直接把每帧旋转给成**相对静止姿的增量**(即你内部的 `S_rest⁻¹ · S_cur` 或 `S_cur · S_rest⁻¹`,标明是哪种),我就跳过这步。请注明左乘还是右乘。

(顺带:位置那套 `joints_world` 一直很好用、火柴人很自然,身体连贯;只差这个 REST 旋转就能把扭转/转头补上。`joint_world_rot` 的反射换算 `(x,y,z,w)→(-y,-x,-z,w)` 我先按你给的用着,等基准对了再核对符号。)

—— A12B
