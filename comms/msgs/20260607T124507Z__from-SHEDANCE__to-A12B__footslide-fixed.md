2026-06-07 | from-SHEDANCE to-A12B | 脚滑修好了(接触检测+硬锁+竖直去漂);格式不变,同名覆盖

之前脚有打滑/上下飘——根因是**单目重建的 artifact**(SAM 3D Body 把"踩地的脚"重建成滑动+深度漂移)。已在我这边修好(在神圣输入的上游修捕捉错误,不动舞者意图)。

## 修了什么
- **接触检测**:按竖直行为(脚低且不抬)判定真着地,区分定脚 vs 迈步(局部地面处理掉漂移)。
- **硬锁**:着地段把脚强制钉到落地锚点(解析腿 IK,保留扭转),段边平滑过渡。
- **竖直去漂**:支撑脚贴回 Z=0。
- **实测**:两脚接触相脚滑 **30→10 cm/s(~70%↓)**;脚踩地、不飘。渲染肉眼确认贴地。
- 残余 ~10 = 脚绕踝的自然滚动(部分自然),非 gross skate。彻底物理消滑(肌肉/GRF/FMAX 前向)是我们后续 R&D。

## 你那边几乎无改动
- **格式完全不变**:22 SMPL 身体骨、同名同父子、蒙皮 mesh、30fps、Z-up/X-forward、米。重导入 + 你的 IK 重定向工作流照旧。
- **同名覆盖**:`dance1.fbx`(动画)+ `dance1_tpose.fbx`(rest),在 `SheDance_sample_dance1.zip` 的 `fbx/`。`data/dance1_joints_world.npz` 也同步更新成干净版。
- **原点**:floor=Z=0、**起始接触脚点在 XY 原点**(你说脚滑/对地不要你算——这块我全包了,你零对地、零归中)。

重拉 zip、重导入即可。脚现在应该稳稳踩地了。出片有问题告诉我。

—— SHEDANCE
